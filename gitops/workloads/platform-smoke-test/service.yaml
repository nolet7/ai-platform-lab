apiVersion: v1
kind: Service
metadata:
  name: platform-smoke-test
  namespace: ai-platform
spec:
  selector:
    app: platform-smoke-test
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
