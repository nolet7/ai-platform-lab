apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-edge-tls
  namespace: observability
spec:
  secretName: prometheus-edge-tls
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - prometheus.ai-platform.local
  privateKey:
    algorithm: RSA
    size: 2048
    rotationPolicy: Always
  usages:
    - server auth
    - digital signature
    - key encipherment
  issuerRef:
    name: ai-platform-ca
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus-clean-url
  namespace: observability
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - prometheus.ai-platform.local
      secretName: prometheus-edge-tls
  rules:
    - host: prometheus.ai-platform.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kube-prometheus-stack-prometheus
                port:
                  number: 9090
