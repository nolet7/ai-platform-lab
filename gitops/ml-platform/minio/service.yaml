apiVersion: v1
kind: Service

metadata:
  name: minio

spec:
  type: ClusterIP

  selector:
    app: minio

  ports:
    - name: s3
      port: 9000
      targetPort: s3

    - name: console
      port: 9001
      targetPort: console
